[project]
name = "cs-match-tracker"
version = "0.1.0"
description = "A local application for tracking Counter-Strike teams and comparing their performance."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "ruff>=0.16.8",
]
